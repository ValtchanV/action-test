name: Node.js CI Pipeline

on:
  pull_request:
    branches:
      - main
      - master
      - develop

jobs:
  use-template:
    uses: ./.github/workflow-templates/node-ci.yml
    with:
      node-version: '22'
      run-test: true
